% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hedge_effectiveness_dcc.R
\name{hedge_effectiveness_dcc}
\alias{hedge_effectiveness_dcc}
\title{Hedge Ratio, Hedging Effectiveness, and Optimal Hedging Weight}
\usage{
hedge_effectiveness_dcc(
  hedged,
  hedge,
  arma_order = c(1, 0),
  garch_order = c(1, 1),
  univariate_dist = "norm",
  dcc_dist = "mvnorm",
  dcc_model = "aDCC"
)
}
\arguments{
\item{hedged}{Numeric vector or \code{zoo}/\code{xts} series of returns for the asset
being hedged (asset 1).}

\item{hedge}{Numeric vector or \code{zoo}/\code{xts} series of returns for the
hedging asset (asset 2).}

\item{arma_order}{Integer length-2; ARMA order in the univariate mean
(default \code{c(1, 0)}).}

\item{garch_order}{Integer length-2; GARCH order in the univariate variance
(default \code{c(1, 1)}).}

\item{univariate_dist}{Character; univariate error distribution for
\code{rugarch::ugarchspec()} (default \code{"norm"}).}

\item{dcc_dist}{Character; DCC distribution for \code{rmgarch::dccspec()}
(default \code{"mvnorm"}).}

\item{dcc_model}{Character; DCC model type (default \code{"aDCC"}).}
}
\value{
A one-row \code{data.frame} with:
\describe{
\item{\code{beta_mean}}{Mean hedge ratio \eqn{\bar{\beta}}}
\item{\code{beta_min}}{Minimum hedge ratio}
\item{\code{beta_max}}{Maximum hedge ratio}
\item{\code{HE}}{Single conditional hedging effectiveness value}
\item{\code{OPW}}{Mean optimal portfolio weight of the \strong{hedging} asset (clipped)}
}
}
\description{
Fits a bivariate asymmetric DCC (aDCC) model with GJR–GARCH(1,1) margins and
reports: (i) the time-varying hedge ratio summary (mean/min/max),
(ii) hedging effectiveness (HE) value based on \strong{conditional}
variances, and (iii) the \strong{optimal portfolio weight of the hedging asset}
averaged over time (weights clipped to [0,1]).
}
\details{
Let \eqn{\Sigma_t} be the conditional covariance matrix from the DCC fit with
elements \eqn{\Sigma_{11,t} = \sigma^2_{1,t}}, \eqn{\Sigma_{22,t} = \sigma^2_{2,t}},
and \eqn{\Sigma_{12,t}}. The \strong{time-varying hedge ratio} is
\deqn{\beta_t = \Sigma_{12,t} / \Sigma_{22,t}.}

The \strong{conditional hedging effectiveness (HE)} is reported as a single value:
\deqn{HE \;=\; 1 - \frac{\overline{\operatorname{Var}_t(y_1 - \beta_t y_2)}}%
                        {\overline{\sigma^2_{1,t}}}
     \;=\; 1 - \frac{\overline{\sigma^2_{1,t} - \Sigma_{12,t}^2/\sigma^2_{2,t}}}
                        {\overline{\sigma^2_{1,t}}},}
where the bar denotes the time average.

The \strong{optimal portfolio weight} of the hedged/spot asset \eqn{S} versus the
hedging asset \eqn{F} follows:
\deqn{w_{SF,t} \;=\; \frac{\sigma^2_{F,t} - \Sigma_{SF,t}}
                           {\sigma^2_{S,t} - 2\Sigma_{SF,t} + \sigma^2_{F,t}},}
clipped to \eqn{[0,1]}. The function returns \code{OPW} as the \strong{mean weight
of the hedging asset}, i.e. \eqn{\overline{1 - w_{SF,t}}}.
}
\examples{
\dontrun{
# Hedge stock (SP) with gold (GLD)
hedge_effectiveness_dcc(hedgedata$SP, hedgedata$GLD)
}

}
\references{
Basher, S. A., & Sadorsky, P. (2016).
\emph{Hedging emerging market stock prices with oil, gold, VIX, and bonds: A comparison between DCC, ADCC and GO-GARCH.}
\emph{Energy Economics}, 54, 235–247.
}
\seealso{
\code{\link[rugarch]{ugarchspec}},
\code{\link[rmgarch]{dccspec}}, \code{\link[rmgarch]{dccfit}},
\code{\link[rmgarch]{rcov}}
}
